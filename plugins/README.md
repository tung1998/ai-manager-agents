# plugins

Plugin ngoài core: runtime, storage, notify, trigger source, role template. Đăng ký qua registry tại compile-time (Go build tag / import side-effect). Core không import plugin cụ thể.

- Milestone: M2+
- Xem: docs/INTERFACES.md#plugin
