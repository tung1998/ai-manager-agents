# internal/notify

`NotifyAdapter` + router theo severity/kênh. Discord (M1), Telegram/Slack (M2, plugin). Nhận tin inbound (on-demand ask) qua cùng adapter nếu kênh hỗ trợ.

- Phụ thuộc: storage (audit gửi)
- Milestone: M1
