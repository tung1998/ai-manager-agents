# internal/initscan

Quét tĩnh repo cho `office init`: manifest, SDK, infra, migrations, .mcp.json, .env.example (chỉ tên biến). Detect runtime đã cài + API key có trong env. Sinh bản tóm tắt cho LLM (M3).

- Phụ thuộc: config
- Milestone: M0 (quét tĩnh), M3 (LLM + ước lượng nhân sự + diff)
