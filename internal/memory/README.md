# internal/memory

Ba lớp memory mỗi manager: working, long_term, baseline. Compaction khi vượt ngưỡng (neo vào raw findings/worker outputs, không tóm tắt đệ quy). Baseline thống kê tính bằng code.

- Phụ thuộc: storage, runtime (compaction)
- Milestone: M1 (working + long_term), M5 (learn baseline)
