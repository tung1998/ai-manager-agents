# internal/mcp

Registry MCP server khai báo trong config, probe kết nối cho `doctor`, phân loại tool read-only (ưu tiên MCP tool annotations, rồi allowlist tường minh; không đoán theo tên).

- Phụ thuộc: config
- Milestone: M0
