# internal/blackboard

CRUD `threads` và `findings`. Validate evidence bắt buộc (conclusion/hypothesis phải trỏ tới worker_output). Cung cấp view lọc theo round để hỗ trợ phân tích độc lập.

- Phụ thuộc: storage
- Milestone: M1
